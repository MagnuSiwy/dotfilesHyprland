[Unit]
Description=Playerctl daemon to control the media
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/playerctld daemon

[Install]
WantedBy=graphical-session.target
